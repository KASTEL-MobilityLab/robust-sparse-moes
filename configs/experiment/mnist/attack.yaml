# @package _global_

# to execute this experiment run:
# python run.py experiment=example

defaults:
- mnist/default.yaml
- override /model: mnist_attack_lit.yaml

logger:
  wandb:
    tags: ["mnist", "attack"]
